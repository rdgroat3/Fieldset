# Copy to .env and fill in after deploying the Cloudflare Worker in /server
# (see README). .env is gitignored — never commit real values.
# Restart `expo start` after editing.

EXPO_PUBLIC_AI_DECODE_ENDPOINT=
EXPO_PUBLIC_AI_DECODE_TOKEN=
